import { THEME } from "../../theme";
import { Platform } from "react-native";

export const defaultOptions = {
  headerStyle: {
    backgroundColor: THEME.MAIN_COLOR,
    elevation: 0,
    shadowOpacity: 0,
    borderBottomWidth: 0
  },
  headerTintColor: "#fff",
  cardStyle: { backgroundColor: THEME.MAIN_COLOR }
};
